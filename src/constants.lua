return {
    CELL_HEIGHT = 32,
    CELL_WIDTH = 32,
    GRAVITY = 200,
    JUMP_DECAY = 600,
    JUMP_ACCEL = 500,
    JUMP_BUMP_HEAD_DECAY_MODIFIER = 10,
    WALK_SPEED = 175,
    FRICTION = 800,
    FORBIDDEN_HACK_RATIO = 1.25
}
